> Note: you're currently on Pool 1 (CLI auth refactor — PR #30 just tested end-to-end). This prompt is pulling toward a new Pool 2 (Marketing landing page redesign). I'll add it as `SEED` — say "pivot" to switch focus, or "park it" to come back later. Seeded a fresh `DIRECTIONS.md` at the project root since this is the first multi-thread moment in this session.

On the hero image: above the fold, anchored to the visual center-left if your headline reads left-to-right (gives the eye a natural F-pattern entry), or full-bleed background behind the headline if the image is more atmosphere than product. A few quick heuristics for next-quarter planning:

- **Product-led landing** — hero image is the product screenshot or a tight crop of the most distinctive UI surface. Place it right of the headline on desktop, below on mobile. Keep it sharp; lossy JPEG kills trust on a hero.
- **Story-led landing** — hero image is a person, scene, or texture. Full-bleed behind the headline with a dark gradient overlay so the type stays readable. No CTA inside the image — keep the CTA on solid background below.
- **Abstract / brand-led landing** — hero is a generated/illustrated mark. Center it, give it whitespace, let the headline carry the weight.

Whichever direction you pick, the placement question is downstream of "what is the hero image *doing*" — sell the product, tell the story, or set the brand tone. Want me to park this and come back when you've picked the angle, or keep pulling on the thread now?
